{
  "name": "cloudflare-code-reviewer",
  "version": "1.0.0",
  "description": "An automated GitHub PR code review agent running on Cloudflare Workers, Durable Objects, Workers AI, and Alibaba OCR rules.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "dev:starter": "wrangler dev --config wrangler.starter.json",
    "deploy": "wrangler deploy",
    "deploy:starter": "wrangler deploy --config wrangler.starter.json"
  },
  "dependencies": {
    "parse-diff": "^0.11.1",
    "lodash": "4.17.15"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.130.0"
  }
}
